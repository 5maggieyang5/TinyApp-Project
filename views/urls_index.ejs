<!DOCTYPE html>

<html lang="en">

<head>
  <title>TinyApp</title>
</head>


<body>
  <!-- Add your EJS code here -->

  <% include ./partials/_header %>

  <ul>
    <% for(var url in urls){ %>
      <li><%=`${url}:`%> <a href="<%=`${urls[url]}`%>"><%=`${urls[url]}`%></a></li>

      <form method="POST" action="/urls/<%=url%>/delete">
          <a href="/urls/<%=url%>/update">Edit</a>
          <button type="submit">Delete</button>
      </form>

    <% } %>
  </ul>

  <a href="urls/new">A link for shortening a new URL</a>

</body>


</html>
