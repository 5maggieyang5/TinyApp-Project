<!DOCTYPE html>

<html lang="en">

<head>
  <title>TinyApp</title>
</head>


<body>

<% if (user){ %>

  <% include ./partials/_header %>

    <ul>
      <% for(var url in urls){ %>
        <li><a href="<%=`${urls[url].longURL}`%>"><%=`${url}`%></a> -----> <a href="<%=`${urls[url].longURL}`%>"><%=`${urls[url].longURL}`%></a></li>

        <form method="POST" action="/urls/<%=url%>/delete">
          <input type="submit" name="edit" value="Edit" formaction="/urls/<%= url%>" formmethod="GET">
          <button type="submit">Delete</button>
        </form>
      <% } %>
    </ul>

<% }else{ %>

    <% include ./partials/_header %>

<% } %>

  <a href="urls/new">Shortening a new URL</a>

</body>
</html>
